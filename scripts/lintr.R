# Install the lintr package
#install.packages("lintr")

# Load the lintr library
library(lintr)

# Lint through all the scripts in the current directory
lint_dir(path = ".")

#Remember to fix errors manually!


